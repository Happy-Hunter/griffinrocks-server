<%= form_with(model: rock, local: true) do |form| %>
  <% if rock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rock.errors.count, "error") %> prohibited this rock from being saved:</h2>

      <ul>
      <% rock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
